<Window x:Class="ZCompare.App.ProfileNameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="保存对比配置"
        Width="420"
        Height="170"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="配置名称" FontWeight="SemiBold" />
        <TextBox x:Name="ProfileNameTextBox"
                 Grid.Row="1"
                 Margin="0,8,0,0"
                 Padding="8,5"
                 AutomationProperties.Name="配置名称"
                 MaxLength="80" />
        <StackPanel Grid.Row="2"
                    Margin="0,16,0,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Orientation="Horizontal">
            <Button Width="82" Margin="0,0,8,0" IsCancel="True" Content="取消" />
            <Button Width="82" Click="SaveButton_OnClick" IsDefault="True" Content="保存" />
        </StackPanel>
    </Grid>
</Window>
